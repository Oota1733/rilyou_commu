<table class='table'>
  <thead>
    <tr>
      <th></th>
      <th>グループ名</th>
      <th>紹介文</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @communities.each do |community| %>
      <tr>
        
        <td><%= link_to community.name,community_path(community) %></td>
        <td><%= community.introduction %></td>
        <% if community.is_owned_by?(current_user) %>
          <td><%= link_to 'Edit', edit_community_path(group), class: "community_#{community.id} btn btn-sm btn-success" %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table> 