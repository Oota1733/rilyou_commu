<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h3>Editing Notification</h3>
      <%= form_with model: @notification, url: notification_path(@notification) do |f| %>
        <%= render 'form_edit', f: f %>
      <% end %>
    </div>
  </div>
</div>