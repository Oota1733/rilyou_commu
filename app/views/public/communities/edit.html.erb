<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h3>Editing Group</h3>
      <%= form_with model: @community, url: community_path(@community) do |f| %>
        <%= render 'form', f: f %>
      <% end %>
    </div>
  </div>
</div>