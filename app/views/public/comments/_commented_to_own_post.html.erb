<%= link_to community_post_path(notice.subject.post.community_id, notice.subject.post.id) do %>
  <div>
    <%= link_to notice.subject.end_user.first_name, user_path(notice.subject.end_user) %>
    があなたの
    <%= link_to '投稿', post_path(notice.subject.post_workout) %>
    にコメントしました
    <%= " (#{time_ago_in_words(notice.created_at)}前)" %>
  </div>
<% end %>