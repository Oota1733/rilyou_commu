<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h3>Editing Post</h3>
      <%= form_with model: @post, url: community_post_path(@post.community, @post) do |f| %>
        <%= render 'form', f: f %>
      <% end %>
    </div>
  </div>
</div>